import React from "react"
import styles from "./welcome-sequence.module.scss"

export interface WelcomeSequenceProps {

}

export const WelcomeSequence = (props: WelcomeSequenceProps) => {
    return (
        <div className={styles["container"]}>
            test
        </div>
    )
}